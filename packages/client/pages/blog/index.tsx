import styled from 'styled-components';

const StyledPage = styled.div`
  .page {
  }
`;

const BlogPage = () => {
  return (
    <StyledPage>
      Blog
    </StyledPage>
  );
}

export default BlogPage;
