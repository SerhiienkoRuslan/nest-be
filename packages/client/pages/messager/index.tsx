import styled from 'styled-components';

const StyledPage = styled.div`
  .page {
  }
`;

const MessagerPage = () => {
  return (
    <StyledPage>
      Messager
    </StyledPage>
  );
}

export default MessagerPage;
